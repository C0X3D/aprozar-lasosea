import { ProductFilterPipe } from './product-filter-pipe-pipe';

describe('ProductFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
